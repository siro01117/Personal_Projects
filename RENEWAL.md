# ra-kan.cloud 전면 리뉴얼 — 사양

2026-09-09 확정. 구현자는 이 문서를 진실의 출처로 삼는다.

---

## 0. 무엇이 바뀌는가

지금 포털은 프로젝트 카드가 평평하게 나열되고, 일부 카드만 SHA-256 해시로 가려져 있다.
그 해시는 번들에 그대로 노출돼 잠금 구실을 못 한다 (기존 코드 주석도 그렇게 인정한다).

바뀌는 것 세 가지:
1. **가짜 잠금 제거 → 진짜 로그인.** Supabase Auth로 사이트 전체를 막는다.
2. **카테고리 도입.** 기존 모듈 = `헤리티지`, 새로 `학습`.
3. **학습 모듈 신설.** 1학년 2학기 대시보드 + 과목별 상세 + 시험대비.

---

## 1. 인증 — 진짜다

**이미 서버 쪽 준비는 끝나 있다** (2026-09-09 적용·검증 완료):

- Supabase Auth 계정: `cto@ra-kan.cloud` / `p11223344$$`
  - `user_metadata.login_id = "cto"`, `name = "나한결"`
- `public.kv`, `public.guest_progress` — RLS 정책은 `authenticated` 만 전체 허용
- `anon` 역할은 GRANT를 **회수**했다. 로그인 전에는 데이터에 손도 못 댄다
- 검증 결과: 로그인 전 읽기 `42501 차단` → 로그인 후 읽기·쓰기 성공

### 화면 동작
- 로그인 화면에서 아이디는 **`cto`** 로 입력받는다. 내부에서 `cto@ra-kan.cloud` 로 바꿔 `signInWithPassword` 호출.
  (사용자에게 이메일을 노출하지 않는다. `@`가 포함된 입력이 오면 그대로 쓴다.)
- **로그인 유지**: `createClient(..., { auth: { persistSession: true, autoRefreshToken: true } })`.
  Supabase JS가 localStorage에 세션을 저장하고 토큰을 자동 갱신한다. 직접 만들지 마라.
- 앱 최상단에서 `supabase.auth.getSession()` 으로 복원 → 세션 없으면 로그인 화면, 있으면 본문.
  `onAuthStateChange` 구독으로 로그아웃·만료를 즉시 반영한다.
- 로그아웃 버튼은 사용자 메뉴 안에.

### 하지 말 것
- 비밀번호를 코드에 넣지 마라. 해시도 넣지 마라. 검증은 전부 Supabase가 한다.
- 기존 `ADMIN_HASH`, `AUTH_KEY`, 관리자 토글 로직은 **삭제**한다.

### 남는 한계 (알고 있어야 함)
정적 사이트라 `out/` 안의 HTML·JS 자체는 URL을 알면 누구나 받을 수 있다.
로그인이 지키는 것은 **데이터**(Supabase의 학습 내용)이지 페이지 껍데기가 아니다.
페이지 껍데기까지 막으려면 Vercel Deployment Protection을 켜야 한다 — 별도 결정 사항.

---

## 2. 정보 구조

```
/                     로그인 게이트 → 통과 시 홈
홈
 ├ 학습               ← 새 카테고리
 │   └ 1학년 2학기     /study
 │        ├ 대시보드   (기본)
 │        ├ 과목 상세  /study?c=<courseId>
 │        └ 시험대비   /study?c=<courseId>&t=exam
 └ 헤리티지           ← 기존 것 전부
     ├ 스터디큐브 모듈 (/m/*)
     └ 프로젝트 카드   (public/projects/*)
```

- 카테고리는 홈에서 **탭 또는 섹션**으로 나눈다. 학습이 먼저(지금 매일 쓰는 것), 헤리티지가 뒤.
- 라우팅은 정적 추출 제약상 **쿼리스트링**을 쓴다. 동적 세그먼트(`/study/[id]`)는
  `generateStaticParams` 없이는 export 되지 않는다. 과목이 늘어날 수 있으므로 쿼리로 간다.

---

## 3. 학습 모듈 `/study`

### 데이터
- **원본**: Supabase `kv` 테이블, key = `study.2026-2`, value = 시드 JSON과 같은 스키마
- **초기 적재**: `public/study-seed.json` 을 읽어 kv에 없으면 넣는다 (최초 1회)
- **갱신**: 화면에서 편집 → `kv` upsert. 재배포 없이 즉시 반영된다
- 저장은 **낙관적 업데이트 + 실패 시 되돌리기**. 저장 중·저장됨·실패를 항상 보여준다
- 오프라인이거나 저장 실패면 localStorage에 임시 보관하고 다음 접속 때 재시도

### 3-1. 대시보드 (기본 화면)
- **오늘** — 오늘 요일의 수업을 시간순으로. 지금 진행 중인 수업은 강조
- **이번 주** — 주차 번호와 요일별 수업
- **할 일** — `todos` 를 마감 순. 마감 지난 것은 위로, 완료는 접어둠. 체크하면 즉시 저장
- **충돌 경고** — `conflicts` 를 보여준다 (과제·시험이 겹치는 주차)
- **과목 그리드** — 7과목 카드. 과목색 점, 다음 수업, 미정리 차시 수
- 정보가 없는 과목(`confirmed:false`)은 **확인 필요** 배지

### 3-2. 과목 상세
탭 구성: `개요` / `진도` / `자료` / `시험대비`
- **개요** — 교수·시간·강의실·평가 비중(막대)·교재·교수 스타일·주의사항·전략
- **진도** — 16주 표. 각 주차에 차시 노트를 연결. 오늘이 몇 주차인지 표시
- **자료** — `materials` 그룹별 목록
- **시험대비** — 아래 3-3

### 3-3. 시험대비
- 과목별 시험 정보(유형·범위·난이도)
- **요약 카드**: 주차별/단원별 정리 내용을 카드로. 접었다 펼 수 있게
- **오답·헷갈림 메모**: 자유 입력, 저장
- 내용이 없으면 빈 상태로 "아직 정리한 내용이 없습니다 — 강의대 앱에서 정리하면 여기에 나타납니다"

### 3-4. 확장성 (중요)
- 학기를 추가할 수 있어야 한다. `kv` 키가 `study.<semesterId>` 이므로 **학기 선택기**를 둔다
- 과목·차시·요약은 전부 배열이다. 스키마에 없는 필드가 들어와도 **화면이 깨지지 않게** 방어적으로 렌더한다
- 새 탭을 추가하기 쉽게 탭 정의를 배열로 둔다

---

## 4. 기기 — 모바일·PC·태블릿

이 사이트는 셋 다에서 쓰인다. 하나를 축소한 것이 아니라 각각 제대로 동작해야 한다.

| 폭 | 레이아웃 |
|---|---|
| < 640 (모바일) | 단일 컬럼. 하단 탭바(대시보드·과목·할일·설정). 카드 세로 스택 |
| 640~1024 (태블릿) | 2컬럼. 좌측 과목 목록 + 우측 본문. 하단 탭바 대신 상단 탭 |
| > 1024 (PC) | 3컬럼 가능. 사이드바 고정 + 본문 + 보조 패널 |

- 터치 대상 최소 44x44
- `env(safe-area-inset-*)` 반영 (아이폰 노치·홈 인디케이터)
- 가로/세로 전환에서 레이아웃이 깨지지 않을 것
- 스크롤 컨테이너는 `overscroll-behavior: contain`

### 애플펜슬 (아이패드)
필기 입력을 받는 곳(시험대비 메모, 차시 노트)에 **필기 캔버스**를 둔다.

- Pointer Events 사용: `pointerType === 'pen'` 을 구분한다
- **손바닥 거치 방지**: 펜이 한 번이라도 감지되면 그 이후 `touch` 입력은 그리기에서 무시한다
  (손가락은 스크롤·확대만). 이게 없으면 아이패드에서 못 쓴다
- `pressure` 로 선 굵기 변화 (0.5~2.0배). `coalesced events`(`getCoalescedEvents`)로 선을 매끄럽게
- 도구: 펜 / 형광펜(반투명·곱하기 합성) / 지우개 / 실행취소 / 전체 지우기
- 저장은 **벡터 스트로크 배열**(점 좌표 + 압력)로 kv에 넣는다. PNG로 굽지 마라 — 용량과 확대 품질 문제
- `touch-action: none` 은 캔버스에만 준다. 페이지 전체에 주면 스크롤이 죽는다
- 캔버스는 화면 폭에 맞춰 리사이즈하되 **좌표는 정규화**해 저장한다 (다른 기기에서 열어도 맞게)

---

## 5. 디자인

전면 리뉴얼이다. 기존 포털 스타일에 얽매이지 마라.

- **다크 우선**, 라이트도 동작. 시스템 설정 따라가되 수동 토글 유지 (localStorage)
- **이모지 금지.** 인라인 SVG 라인 아이콘만 (stroke 1.5, currentColor, fill none)
- 과목 색은 아래 OKLCH 7색을 쓴다. 점·라인·막대에만. 배경을 칠하지 마라
  - 라이트: `#cc5355 #b66e00 #6c8f00 #009c7d #008fc8 #6f72da #b259ab`
  - 다크: `#f98281 #e19b2a #96ba4a #00c8a8 #00bbf2 #989fff #de87d5`
  - **금지**: `#6366F1` `#8B5CF6` `#A855F7` (AI 생성 UI의 대표 지문)
- 시각·날짜·수치는 등폭 + `tabular-nums`. **단, 한글을 등폭으로 감싸지 마라** (자간이 벌어져 깨진다)
- Tailwind 4가 이미 설치돼 있다. 그대로 쓴다
- `framer-motion` 있음 — 전환에 절제해서 쓴다. 140~180ms
- `lucide-react` 있음 — 아이콘은 여기서 가져다 쓰면 된다 (라인 아이콘, 이모지 아님)
- 빈 상태를 반드시 디자인한다. 무엇을 해야 하는지 한 줄로
- `prefers-reduced-motion` 존중

---

## 6. 기술 제약

- Next 16 `output: 'export'` — **서버 컴포넌트에서 데이터 못 가져온다.** 전부 클라이언트에서 Supabase 호출
- 미들웨어·서버액션 사용 불가
- 빌드는 `npm run build` (webpack 고정 — Turbopack은 PGlite를 깨뜨린다. 바꾸지 마라)
- 기존 `/m/*` 스터디큐브 모듈과 `public/projects/*` 는 **건드리지 마라.** 헤리티지로 분류만 한다
- `lib/scan.js` 의 프로젝트 자동 스캔은 유지한다

## 7. 하지 말 것

- 비밀번호·해시를 코드에 넣기
- 이모지
- `anon` 역할에 권한 다시 주기
- 필기를 PNG로 저장하기
- 페이지 전체에 `touch-action: none`
- 기존 스터디큐브 모듈 수정
